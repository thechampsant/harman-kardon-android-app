<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <style name="ApplicationStyle" parent="AppBaseTheme">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@drawable/page2bg</item>
        <item name="android:buttonStyle">@style/CKButton</item>
    </style>

    <style name="CKButton" parent="android:style/Widget.Button">
        <item name="android:background">@drawable/custom_button</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textStyle">bold</item>
        <item name="android:shadowColor">#000000</item>
        <item name="android:shadowDx">1</item>
        <item name="android:shadowDy">2</item>
        <item name="android:shadowRadius">2</item>
        <item name="android:paddingLeft">@dimen/btnpadding</item>
        <item name="android:paddingRight">@dimen/btnpadding</item>
        <item name="android:minWidth">90dp</item>

        <!-- <item name="android:textAlignment">center</item> -->
        <item name="android:typeface">serif</item>
    </style>

</resources>